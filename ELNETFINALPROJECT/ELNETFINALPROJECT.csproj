<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
  </ItemGroup>
  <!-- Removed EF Core package references which caused restore/build failures in this environment.
       Re-add these if your application requires Entity Framework Core and your environment can restore NuGet packages. -->

</Project>
